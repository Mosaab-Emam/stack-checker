#[derive(Debug)]
pub enum ProgrammingLanguage {
    PHP,
    Python,
    Javascript,
}

#[derive(Debug)]
pub enum PackageManager {
    Composer,
    Pip,
    Npm,
    Yarn,
    Pnpm,
}

#[derive(Debug)]
pub enum PHPFramework {
    Laravel,
}
